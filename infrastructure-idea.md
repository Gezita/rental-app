# Infrastructure

The app is a TypeScript monolith so I am thinking it should be hostsed on AWS or digital ocean.

# Requirements
- database | a place to store each accounts data
- load balancer
- a VPC
